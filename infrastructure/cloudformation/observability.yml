AWSTemplateFormatVersion: '2010-09-09'

Description: >
  Observabilite du pipeline (EPIC CICD-EP-04 du cahier des charges) :
  - Lambda qui publie 3 metriques custom (PipelineDuration, PipelineSuccess,
    PipelineFailure) a chaque fin d'execution du pipeline, declenchee par
    une regle EventBridge dediee (CodePipeline ne publie pas nativement de
    metrique de duree/succes en CloudWatch).
  - Dashboard CloudWatch : metriques pipeline (custom), CodeBuild, ECS et ALB.
  - 2 alarmes CloudWatch (duree > seuil, echec de pipeline) notifiant le
    topic SNS deja cree dans pipeline.yml.

# ------------------------------------------------------------------
# NOTE SUR L'ORDRE DE DePLOIEMENT (voir ecs-cluster.yaml pour la liste
# complete des 12 stacks) :
#   ... 10. pipeline.yml   11. ecs-autoscaling.yaml
#   12. observability.yml  <- cette stack (importe les exports de
#      pipeline.yml : topic SNS, noms ECS, ALB/target group)
# ------------------------------------------------------------------

Parameters:
  ProjectName:
    Type: String
    Default: taskmanager
    AllowedPattern: '^[a-z][a-z0-9-]*$'
    Description: Doit correspondre au ProjectName des autres stacks.

  Environment:
    Type: String
    Default: dev
    AllowedValues: [dev, staging, prod]
    Description: Doit correspondre a l'Environment des autres stacks.

  PipelineDurationThresholdSeconds:
    Type: Number
    Default: 900
    Description: >
      Seuil d'alarme sur la duree du pipeline, en secondes (F4 du cahier
      des charges : "alarme si la duree depasse 15 minutes" -> 900s).

  AlarmEmail:
    Type: String
    Default: ''
    Description: >
      Adresse email optionnelle a notifier (US-04 : "je reçois un email
      d'alerte"). Laisser vide pour ne creer aucun abonnement — le topic
      SNS existe deja (pipeline.yaml) et peut être abonne manuellement
      (email, Slack via chatbot...) apres coup sans redeployer cette stack.

Conditions:
  HasAlarmEmail: !Not [!Equals [!Ref AlarmEmail, '']]

Resources:

  # ==========================================================================
  # LAMBDA — publication des metriques custom du pipeline
  # CodePipeline ne publie pas nativement de metrique de duree/succes en
  # CloudWatch (contrairement a CodeBuild, qui publie deja Duration/
  # SucceededBuilds/FailedBuilds dans AWS/CodeBuild — recuperees directement
  # dans le dashboard plus bas, SANS Lambda). Cette fonction comble ce trou
  # precisement pour CICD-TK-18 ("definir les metriques custom").
  # ==========================================================================
  MetricsPublisherLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub '/aws/lambda/${ProjectName}-${Environment}-pipeline-metrics'
      RetentionInDays: 30

  MetricsPublisherRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub '${ProjectName}-${Environment}-pipeline-metrics-role'
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: !Sub '${ProjectName}-${Environment}-pipeline-metrics-policy'
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Sid: WriteOwnLogs
                Effect: Allow
                Action:
                  - logs:CreateLogStream
                  - logs:PutLogEvents
                Resource:
                  - !GetAtt MetricsPublisherLogGroup.Arn
              - Sid: ReadPipelineExecutions
                Effect: Allow
                Action:
                  - codepipeline:ListPipelineExecutions
                Resource:
                  - !Sub 'arn:aws:codepipeline:${AWS::Region}:${AWS::AccountId}:${ProjectName}-${Environment}-pipeline'
              - Sid: PublishCustomMetrics
                Effect: Allow
                Action:
                  - cloudwatch:PutMetricData
                # cloudwatch:PutMetricData n'accepte pas de restriction par
                # ARN (contrainte AWS) ; portee limitee par une Condition
                # sur le namespace pour eviter toute publication hors sujet.
                Resource: '*'
                Condition:
                  StringEquals:
                    cloudwatch:namespace: !Sub '${ProjectName}/${Environment}/Pipeline'
      Tags:
        - Key: Project
          Value: !Ref ProjectName
        - Key: Environment
          Value: !Ref Environment

  MetricsPublisherFunction:
    Type: AWS::Lambda::Function
    DependsOn: MetricsPublisherLogGroup
    Properties:
      FunctionName: !Sub '${ProjectName}-${Environment}-pipeline-metrics'
      Runtime: python3.12
      Handler: index.handler
      Role: !GetAtt MetricsPublisherRole.Arn
      Timeout: 30
      MemorySize: 128
      Environment:
        Variables:
          METRICS_NAMESPACE: !Sub '${ProjectName}/${Environment}/Pipeline'
      Code:
        ZipFile: |
          import os
          import boto3

          codepipeline = boto3.client("codepipeline")
          cloudwatch = boto3.client("cloudwatch")
          NAMESPACE = os.environ["METRICS_NAMESPACE"]


          def handler(event, context):
              detail = event["detail"]
              pipeline_name = detail["pipeline"]
              execution_id = detail["execution-id"]
              state = detail["state"]  # SUCCEEDED ou FAILED

              metrics = [
                  {
                      "MetricName": "PipelineSuccess",
                      "Value": 1 if state == "SUCCEEDED" else 0,
                      "Unit": "Count",
                  },
                  {
                      "MetricName": "PipelineFailure",
                      "Value": 1 if state == "FAILED" else 0,
                      "Unit": "Count",
                  },
              ]

              # Duree = lastUpdateTime - startTime de CETTE execution precise
              # (list_pipeline_executions est la seule API CodePipeline qui
              # expose ces deux timestamps ensemble).
              try:
                  paginator = codepipeline.get_paginator("list_pipeline_executions")
                  for page in paginator.paginate(pipelineName=pipeline_name):
                      for execution in page.get("pipelineExecutionSummaries", []):
                          if execution.get("pipelineExecutionId") == execution_id:
                              start = execution["startTime"]
                              end = execution["lastUpdateTime"]
                              duration_seconds = (end - start).total_seconds()
                              metrics.append(
                                  {
                                      "MetricName": "PipelineDuration",
                                      "Value": duration_seconds,
                                      "Unit": "Seconds",
                                  }
                              )
                              break
              except Exception as exc:  # noqa: BLE001
                  print(f"Impossible de calculer la duree du pipeline: {exc}")

              cloudwatch.put_metric_data(Namespace=NAMESPACE, MetricData=metrics)
              return {"status": "ok", "metricsPublished": len(metrics)}
      Tags:
        - Key: Project
          Value: !Ref ProjectName
        - Key: Environment
          Value: !Ref Environment

  MetricsPublisherPermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref MetricsPublisherFunction
      Action: lambda:InvokeFunction
      Principal: events.amazonaws.com
      SourceArn: !GetAtt PipelineMetricsEventRule.Arn

  # Regle EventBridge DeDIeE (distincte de PipelineStateChangeRule dans
  # pipeline.yaml, qui cible le topic SNS) : ne reagit qu'aux 2 etats
  # "terminaux" (duree connue), pas a STARTED.
  PipelineMetricsEventRule:
    Type: AWS::Events::Rule
    Properties:
      Name: !Sub '${ProjectName}-${Environment}-pipeline-metrics-trigger'
      Description: Declenche la publication des metriques custom du pipeline.
      EventPattern:
        source:
          - aws.codepipeline
        detail-type:
          - 'CodePipeline Pipeline Execution State Change'
        detail:
          pipeline:
            - !Sub '${ProjectName}-${Environment}-pipeline'
          state:
            - SUCCEEDED
            - FAILED
      Targets:
        - Arn: !GetAtt MetricsPublisherFunction.Arn
          Id: MetricsPublisherTarget

  # ==========================================================================
  # ALARMES CLOUDWATCH (CICD-TK-20) -> notifient le topic SNS de pipeline.yaml
  # ==========================================================================
  PipelineDurationAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub '${ProjectName}-${Environment}-pipeline-duration-alarm'
      AlarmDescription: >
        Duree du pipeline au-dela du seuil (F4 : 15 min par defaut). Base
        sur la metrique custom PipelineDuration (publiee par
        MetricsPublisherFunction a chaque fin d'execution) — ne peut donc se
        declencher qu'APReS coup, pas pendant un pipeline encore en cours.
      Namespace: !Sub '${ProjectName}/${Environment}/Pipeline'
      MetricName: PipelineDuration
      Statistic: Maximum
      Period: 300
      EvaluationPeriods: 1
      Threshold: !Ref PipelineDurationThresholdSeconds
      ComparisonOperator: GreaterThanThreshold
      TreatMissingData: notBreaching
      AlarmActions:
        - !ImportValue
          'Fn::Sub': '${ProjectName}-${Environment}-pipeline-notifications-arn'

  PipelineFailureAlarm:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub '${ProjectName}-${Environment}-pipeline-failure-alarm'
      AlarmDescription: >
        Au moins un echec de pipeline sur la periode — vient s'ajouter a la
        notification SNS immediate (EventBridge, pipeline.yaml) avec un
        signal agrege visible dans CloudWatch/le dashboard.
      Namespace: !Sub '${ProjectName}/${Environment}/Pipeline'
      MetricName: PipelineFailure
      Statistic: Sum
      Period: 300
      EvaluationPeriods: 1
      Threshold: 1
      ComparisonOperator: GreaterThanOrEqualToThreshold
      TreatMissingData: notBreaching
      AlarmActions:
        - !ImportValue
          'Fn::Sub': '${ProjectName}-${Environment}-pipeline-notifications-arn'

  # ==========================================================================
  # ABONNEMENT EMAIL OPTIONNEL (US-04) sur le topic SNS existant
  # ==========================================================================
  AlarmEmailSubscription:
    Type: AWS::SNS::Subscription
    Condition: HasAlarmEmail
    Properties:
      TopicArn: !ImportValue
        'Fn::Sub': '${ProjectName}-${Environment}-pipeline-notifications-arn'
      Protocol: email
      Endpoint: !Ref AlarmEmail

  # ==========================================================================
  # DASHBOARD CLOUDWATCH (CICD-TK-19) — US-04 : duree moyenne, taux de
  # succes (7j glissants), nombre de deploiements, + CodeBuild/ECS/ALB.
  # ==========================================================================
  PipelineDashboard:
    Type: AWS::CloudWatch::Dashboard
    Properties:
      DashboardName: !Sub '${ProjectName}-${Environment}-dashboard'
      # Forme a 2 arguments de Fn::Sub : un Fn::Sub simple ne sait resoudre
      # que des Parameters/pseudo-parametres/Ref/GetAtt de CE template — pas
      # des valeurs importees d'une autre stack. La map (2e element de la
      # liste) injecte EcsClusterName/EcsServiceName/AlbFullName/
      # BlueTargetGroupFullName (tous importes de pipeline.yaml) comme
      # variables de substitution supplementaires.
      DashboardBody: !Sub
        - |
          {
            "widgets": [
              {
                "type": "text",
                "x": 0, "y": 0, "width": 24, "height": 1,
                "properties": {
                  "markdown": "# ${ProjectName} (${Environment}) — Pipeline CI/CD"
                }
              },
              {
                "type": "metric",
                "x": 0, "y": 1, "width": 8, "height": 6,
                "properties": {
                  "title": "Duree du pipeline (s)",
                  "view": "timeSeries",
                  "region": "${AWS::Region}",
                  "metrics": [
                    [ "${ProjectName}/${Environment}/Pipeline", "PipelineDuration", { "stat": "Maximum" } ]
                  ],
                  "annotations": {
                    "horizontal": [
                      { "label": "Seuil alarme", "value": ${PipelineDurationThresholdSeconds} }
                    ]
                  }
                }
              },
              {
                "type": "metric",
                "x": 8, "y": 1, "width": 8, "height": 6,
                "properties": {
                  "title": "Deploiements (succes vs echecs)",
                  "view": "timeSeries",
                  "stacked": true,
                  "region": "${AWS::Region}",
                  "metrics": [
                    [ "${ProjectName}/${Environment}/Pipeline", "PipelineSuccess", { "stat": "Sum", "label": "Succes" } ],
                    [ "${ProjectName}/${Environment}/Pipeline", "PipelineFailure", { "stat": "Sum", "label": "echecs" } ]
                  ]
                }
              },
              {
                "type": "metric",
                "x": 16, "y": 1, "width": 8, "height": 6,
                "properties": {
                  "title": "Taux de succes (7 j glissants, %)",
                  "view": "singleValue",
                  "region": "${AWS::Region}",
                  "period": 604800,
                  "metrics": [
                    [ "${ProjectName}/${Environment}/Pipeline", "PipelineSuccess", { "stat": "Sum", "id": "m1", "visible": false } ],
                    [ "${ProjectName}/${Environment}/Pipeline", "PipelineFailure", { "stat": "Sum", "id": "m2", "visible": false } ],
                    [ { "expression": "100*(m1/(m1+m2))", "label": "Taux de succes (%)", "id": "e1" } ]
                  ]
                }
              },
              {
                "type": "metric",
                "x": 0, "y": 7, "width": 8, "height": 6,
                "properties": {
                  "title": "CodeBuild — duree des builds (s)",
                  "view": "timeSeries",
                  "region": "${AWS::Region}",
                  "metrics": [
                    [ "AWS/CodeBuild", "Duration", "ProjectName", "${ProjectName}-${Environment}-build", { "stat": "Average" } ]
                  ]
                }
              },
              {
                "type": "metric",
                "x": 8, "y": 7, "width": 8, "height": 6,
                "properties": {
                  "title": "CodeBuild — builds reussis / echoues",
                  "view": "timeSeries",
                  "stacked": true,
                  "region": "${AWS::Region}",
                  "metrics": [
                    [ "AWS/CodeBuild", "SucceededBuilds", "ProjectName", "${ProjectName}-${Environment}-build", { "stat": "Sum", "label": "Reussis" } ],
                    [ "AWS/CodeBuild", "FailedBuilds", "ProjectName", "${ProjectName}-${Environment}-build", { "stat": "Sum", "label": "echoues" } ]
                  ]
                }
              },
              {
                "type": "metric",
                "x": 16, "y": 7, "width": 8, "height": 6,
                "properties": {
                  "title": "ECS Fargate — CPU / Memoire (%) et nombre de taches",
                  "view": "timeSeries",
                  "region": "${AWS::Region}",
                  "metrics": [
                    [ "AWS/ECS", "CPUUtilization", "ClusterName", "${EcsClusterName}", "ServiceName", "${EcsServiceName}", { "stat": "Average", "label": "CPU" } ],
                    [ "AWS/ECS", "MemoryUtilization", "ClusterName", "${EcsClusterName}", "ServiceName", "${EcsServiceName}", { "stat": "Average", "label": "Memoire" } ],
                    [ "ECS/ContainerInsights", "RunningTaskCount", "ClusterName", "${EcsClusterName}", "ServiceName", "${EcsServiceName}", { "stat": "Average", "label": "Taches en cours", "yAxis": "right" } ]
                  ],
                  "annotations": {
                    "horizontal": [
                      { "label": "Cible CPU auto scaling (70%)", "value": 70 }
                    ]
                  }
                }
              },
              {
                "type": "metric",
                "x": 0, "y": 13, "width": 24, "height": 6,
                "properties": {
                  "title": "ALB — latence applicative (s) et hôtes sains (target group Blue)",
                  "view": "timeSeries",
                  "region": "${AWS::Region}",
                  "metrics": [
                    [ "AWS/ApplicationELB", "TargetResponseTime", "LoadBalancer", "${AlbFullName}", { "stat": "Average", "label": "Latence (p50)" } ],
                    [ "AWS/ApplicationELB", "HealthyHostCount", "LoadBalancer", "${AlbFullName}", "TargetGroup", "${BlueTargetGroupFullName}", { "stat": "Average", "label": "Hôtes sains (Blue)", "yAxis": "right" } ]
                  ]
                }
              }
            ]
          }
        - EcsClusterName: !ImportValue
            'Fn::Sub': '${ProjectName}-${Environment}-ecs-cluster-name'
          EcsServiceName: !ImportValue
            'Fn::Sub': '${ProjectName}-${Environment}-ecs-service-name'
          AlbFullName: !ImportValue
            'Fn::Sub': '${ProjectName}-${Environment}-alb-full-name'
          BlueTargetGroupFullName: !ImportValue
            'Fn::Sub': '${ProjectName}-${Environment}-tg-blue-full-name'

Outputs:

  DashboardName:
    Description: Nom du dashboard CloudWatch
    Value: !Sub '${ProjectName}-${Environment}-dashboard'

  MetricsNamespace:
    Description: Namespace CloudWatch des metriques custom du pipeline
    Value: !Sub '${ProjectName}/${Environment}/Pipeline'

  MetricsPublisherFunctionArn:
    Description: ARN de la fonction Lambda de publication des metriques
    Value: !GetAtt MetricsPublisherFunction.Arn
